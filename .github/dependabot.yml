version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: daily
    cooldown:
      default-days: 30
    groups:
      mantine:
        patterns:
          - "@mantine/*"
      everything-else:
        patterns:
          - "*"
